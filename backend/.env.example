# =============================================================================
# DEVELOPMENT ENVIRONMENT (Local Docker)
# =============================================================================
# Copy this file to .env and update with your local values
# =============================================================================

# Database Configuration
DB_HOST=postgres              # Use 'localhost' if running outside Docker
DB_PORT=5432
DB_NAME=timeflow
DB_USER=dev
DB_PASSWORD=user
DB_SSL_ENABLED=false          # true in production

# Server Configuration
PORT=3000
NODE_ENV=development          # development | test | production

# JWT Secret (change in production!)
JWT_SECRET=dev-secret-change-in-production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost

# Email Configuration (MailHog for local development)
# Web UI: http://localhost:8025
EMAIL_FROM=noreply@timeflow.com
APP_URL=http://localhost
SMTP_HOST=mailhog
SMTP_PORT=1025
# SMTP_USER=                  # Leave empty for MailHog
# SMTP_PASSWORD=              # Leave empty for MailHog

# Logging
LOG_LEVEL=debug               # debug | info | warn | error

# =============================================================================
# For TEST environment (CI), use environment variables:
# NODE_ENV=test, DB_HOST=localhost, SMTP_HOST=mock
# =============================================================================
# For PRODUCTION environment, see ENVIRONMENTS.md
# =============================================================================
